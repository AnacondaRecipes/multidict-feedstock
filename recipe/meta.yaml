{% set name = "multidict" %}
{% set version = "4.2.0" %}
{% set sha256 = "24052724195e46872739faa10c611957bbaceae28eec92e1ce49150b115ec5ed" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  skip: true  # [py2k]

requirements:
  host:
    - python
    - cython
    - setuptools
  build:
    - {{ compiler('c') }}
  run:
    - python

test:
  source_files:
    - tests
  requires:
    - pytest
    - psutil
  commands:
    - py.test tests
  imports:
    - multidict
    - multidict._multidict

about:
  home: http://github.com/aio-libs/multidict
  license: Apache 2.0
  license_family: Apache
  license_file: LICENSE
  summary: 'multidict implementation'

  description: |
    Multidicts are useful for working with HTTP headers, URL query args etc.
  doc_url: http://multidict.readthedocs.io/
  dev_url: http://github.com/aio-libs/multidict
  doc_source_url: https://github.com/aio-libs/multidict/tree/master/docs

extra:
  recipe-maintainers:
    - sodre
    - kszucs
